The Moonrise Problem is a subproblem of the [[AI safety|AI safety]] problem where you try to robustly specify a concept and prove that you have done so.

We can specify a model that takes in a normally-distributed noise input and output world states. The model may have latent variables. 