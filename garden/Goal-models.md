[[info, On Goal-Models — LessWrong|source]]

i'm somewhat confused, but some thoughts: 
- an interpretation is that agents don't know exactly what their goals are so they have goal-models instead
- another one is that goal-models is the correct representation, utility functions can't even represent that one
	- ... which means one of VNM assumptions is rejected or we are talking in another ontology entirely
- goal-models are better than utility functions because you can look at its internals? hmm
	- "However, I’m dissatisfied by the idea of defining a world-model as a generative model over observations. It feels analogous to defining a parliament as a generative model over laws. Yes, technically we can think of parliaments as stochastically outputting laws, but actually the interesting part is in _how_ they do so."
- components of a goal-model update in a somewhat local way; components update based on a few other components

related: [[AI safety]]